{
  "name": "express-sample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "export $(cat .env) && NODE_ENV=development nodemon src/server.js",
    "prod": "NODE_ENV=production node src/server.js",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up",
    "test": "curl --data '{\"key\":\"value\"}' --header 'Content-Type: application/json' localhost:3001/wise-webhook"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "body-parser": "^1.20.3",
    "compression": "^1.7.5",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
